import 'package:flutter/material.dart';

import 'app.dart';

// TODO - NOTE: The application has a built in slow motion setting that can be activated, if animation seems slower than they shoul be then you probably have this setting enabled
void main() => runApp(const ReplyApp());
